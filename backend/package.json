{
  "name": "intelliswarm-rag-backend",
  "version": "1.0.0",
  "description": "RAG backend for IntelliSwarm.ai resume chat",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "load-data": "node scripts/load-resume.js",
    "lambda:local": "sam local start-api --port 3001 --region eu-central-2",
    "lambda:deploy": "sam build && sam deploy --region eu-central-2",
    "lambda:deploy:guided": "sam build && sam deploy --guided --region eu-central-2"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "chromadb": "^1.8.1",
    "ollama": "^0.5.0",
    "langchain": "^0.2.0",
    "@langchain/community": "^0.2.0",
    "@langchain/ollama": "^0.0.4",
    "pdf-parse": "^1.1.1",
    "mammoth": "^1.6.0",
    "dotenv": "^16.4.5",
    "uuid": "^9.0.1",
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0",
    "@aws-sdk/client-s3": "^3.600.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}